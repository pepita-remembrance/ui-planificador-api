# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /planificaciones            controllers.PlanificacionesController.all
GET        /planificaciones/:id        controllers.PlanificacionesController.getById(id: Long)